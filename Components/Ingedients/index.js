const addIngredient = require('./addIngredient');
const listIngredients = require('./listIngredients');
const refillIngredient = require('./refillIngredient');

module.exports = {
    addIngredient,
    listIngredients,
    refillIngredient
}